const initialState = {
  tenants: [],
  selectedTenant: null,
};

const TenantReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default TenantReducer;
